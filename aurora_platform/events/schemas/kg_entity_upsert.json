{
  "$id": "kg_entity_upsert",
  "title": "KG Entity Upsert",
  "type": "object",
  "required": ["entity_id", "entity_type", "labels", "properties"],
  "properties": {
    "entity_id": {"type": "string"},
    "entity_type": {"type": "string"},
    "labels": {"type": "array", "items": {"type": "string"}},
    "properties": {"type": "object"},
    "provenance": {
      "type": "object",
      "properties": {
        "source_id": {"type": "string"},
        "url": {"type": "string"},
        "line_span": {"type": "string"}
      }
    }
  }
}
