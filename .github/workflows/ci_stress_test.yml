name: CI Stress Test

on:
  workflow_dispatch:

jobs:
  simulate-multiple-prs:
    runs-on: ubuntu-latest
    steps:
      - name: Simulate PRs
        run: |
          echo "Simulando múltiplos PRs e execuções paralelas..."
          # Aqui você pode adicionar comandos para disparar workflows em paralelo, usar gh api, etc.
      - name: Document Results
        run: |
          echo "Documentar resultados, tempos de execução, falhas e propor correções conforme necessário."
