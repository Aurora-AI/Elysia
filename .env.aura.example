# ===============================
# Aurora — Runtime Environment
# Fonte de verdade: AURA_* (legado NEO4J_* mantido por compat)
# ===============================

# --- Neo4j (aliases Aurora, fonte de verdade) ---
AURA_NEO4J_URI=bolt://localhost:7687
AURA_NEO4J_USERNAME=neo4j
AURA_NEO4J_PASSWORD=neo4j
AURA_NEO4J_HTTP_URL=http://localhost:7474  # usado por seed via HTTP

# --- Fallbacks/legado (mantidos por compatibilidade) ---
# Se você usa ferramentas que esperam NEO4J_*:
NEO4J_URI=${AURA_NEO4J_URI}
NEO4J_USERNAME=${AURA_NEO4J_USERNAME}
NEO4J_PASSWORD=${AURA_NEO4J_PASSWORD}
NEO4J_URL=${AURA_NEO4J_HTTP_URL}

# --- Outras variáveis úteis do projeto ---
PROJECT_NAME=Aurora
ENVIRONMENT=dev
SECRET_KEY=dev

# --- Config para Neo4j AuraDB (opcional; deixe commented até usar cloud) ---
# Preencha estes se for apontar para AuraDB (bolt+s / neo4j+s)
# AURA_NEO4J_URI=neo4j+s://REPLACE_ME.databases.neo4j.io
# AURA_NEO4J_USERNAME=neo4j
# AURA_NEO4J_PASSWORD=REPLACE_ME
# NEO4J_DATABASE=neo4j

# --- Feature flags simples ---
SKIP_NEO4J=false

# --- Google Cloud / Vertex AI Memory Bank (camada opcional) ---
GOOGLE_CLOUD_PROJECT=aurora-dev
GOOGLE_CLOUD_LOCATION=us-central1
# Formato: projects/{project}/locations/{location}/memoryBanks/{id}
VERTEX_MEMORY_BANK_ID=projects/123456/locations/us-central1/memoryBanks/aurora-mem-dev
