services:
  aurora-core:
    # A CORREÇÃO CRÍTICA: Chamamos 'uvicorn' diretamente, pois ele já está no PATH
    # graças à configuração no Dockerfile.
    command: ["uvicorn", "src.aurora_platform.main:app", "--host", "0.0.0.0", "--port", "8080", "--reload"]
